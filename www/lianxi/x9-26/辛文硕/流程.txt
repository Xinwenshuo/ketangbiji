1.遍历目录
    声明一个变量用函数opthinfol打开一个文件目录
    在用函数readdir读取目录中条目,文件太多写不过来就用while循环记住循环的时候要给函数命名一个变量,不然循环执行的时候出现隔行输出,
    最后用函数closedir关闭文件目录.
2.遍历到表格中
    先用函数include运行一个文件. 
    在写一个html表格里面写好需要的表头,
    用函数opthinfol打开文件目录,在用函数readdir读取目录中条目,用while循环记住循环的时候要给函数命名一个变量,
    用自加的方式自加一个序列号
    在把上面的打开目录的循环出来的文件名写上
    在用函数getSize获取文件的大小
    在用函数$filetype显示文件类型((filetype($filepath)=='dir'?'目录':'文件').')
    在用函数date显示创建文件的时间(.date('Y-m-d',filectime($filepath)).)
    在用函数is_readable查看是否可读,((is_readable($filepath)?'可读':'不可读'))
    最后循环结束用colsedir关闭目录
3.统计磁盘大小
    就是用函数disk_free_space()统计可用空间
    用函数disk_total_space()统计整个磁盘的大小
4.统计文件目录里所有文件的大小
    先用函数dirsize($a)统计大小并设一个$a
    在用函数opendir读取文件$a
    在用循环while读取文件目录reddir{在用if判断当文件目录里出现'.'||'..'的时候结束本层循环在用}在声明一个变量将目录名和文件名拼接'/'在判断用函数is_dir判断拼接后的文件是否是文件夹,如果是文件夹就在统计文件夹里的文件大小,如果不是则统计文件大小,记得在循环外部建一个为零的变量,好再循环的时候用于文件和文件夹循环相加,循环结束最后返回值return,
    在运行完毕后用closedir关闭文件目录
    最后输出要dirsize统计的文件夹的

    
    
